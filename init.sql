-- Activer l'extension pgvector
CREATE EXTENSION IF NOT EXISTS vector;

-- Créer la base de données si elle n'existe pas
-- (PostgreSQL crée automatiquement la base avec les variables d'environnement) 